# 0413 병목문제

## 목표
- full online prereq 런에서 `inst_eps` 급락(0.x 포함) 원인 추적
- RAM/OOM(137) 재발 여부 확인
- `graph.path_factor_for_edge` 병목 완화

## 지금까지 행동 기록
1. 메트릭 세분화 추가
- `online_index.on_match_added` 타이밍 분해(local/mapper/propagate)
- `builder.add_match` 경로 분해(watermark/candidate/pair_eval/pending_insert 등)
- `graph_path` skip/candidate/cache hit/miss 계측 추가

2. 정합성 버그 수정
- `has_prereq && !satisfied` 분기에서 `builder.add_match(...)=accepted`일 때
  runner 온라인 상태와 builder 상태 동기화 누락 수정
- 이후 `active_match_count` vs `builder_active_match_count` 괴리 완화

3. 실험/롤백
- 이벤트 스코프 공유캐시, reverse skip 등은 성능/정확도 리스크로 롤백
- 유지: graph_path binding 기반 도달성 prefilter + add_match local cache

4. 운영 편의
- `scripts/run_pipeline_with_progress.sh` 추가
- 10초 단위 진행률/ETA/최종 exit 출력

5. 최근 운영 이슈
- 동시에 2개 full 런이 돌아가며 성능 저하 확인
- 단일 런으로 정리 후에도 특정 구간에서 `inst_eps` 급락
- 병목 지표: `builder_pair_edges_graph_path_pf_compute_*`가 지배적

6. 사용자 요청 반영(현재 턴)
- 실행 중 `run_pipeline` 프로세스 종료 완료
- 후속 개선을 위해 본 문서 작성

## 현재 판단
- BFS/전역탐색 자체는 이미 역색인(`entity_to_hsg_node`, `online_index.mapper_*`)로 상당부분 대체됨
- 남은 핵심 병목은 `graph_path -> path_factor_for_edge` 단가
- 따라서 후보 수를 더 줄이는 룰/바인딩 특화 prefilter가 우선

## 이번 시행 항목
- [진행] `_candidate_match_ids`에 룰 페어 관련성 prefilter 추가
  - 목적: `_pair_edges_bidirectional`에 들어가는 후보 수 자체를 감소
  - 원칙: 의미상 edge가 절대 생기지 않는 rule pair만 제외(정확도 보수)
- [대기] 필요 시 시간예산 기반 graph_path skip 옵션 추가

## 실행 커맨드(패치 후)
```bash
cd /home/work/SIGMA/HOLMES
scripts/run_pipeline_with_progress.sh \
  --events /home/work/SIGMA/datasets/darpa_tc_e3/benchmark/trace_attack_full.jsonl \
  --rules /home/work/SIGMA/HOLMES/rules/darpa_tc_e3_rules.yaml \
  --out /home/work/SIGMA/HOLMES/output/stage2_full_online_v16_rulepair_prefilter \
  --use-online-prereq \
  --ancestor-index-mode lazy \
  --max-graph-path-candidates-per-match 40 \
  --max-graph-path-edges 5000
```

## 시행 결과 (코드 반영)
- 파일: `HOLMES/engine/hsg/builder.py`
  - `candidate_match_ids_filtered_rule_pair_total` 카운터 추가
  - `_pair_can_produce_edge(left_rule_id, right_rule_id)` 캐시 함수 추가
  - `_candidate_match_ids()`에서 rule pair 관련성 prefilter 추가
    - 전방향 prereq가 없고, 역방향 `graph_path`도 불가능한 candidate 제거
- 파일: `HOLMES/engine/stream/runner.py`
  - 신규 메트릭 export 추가
    - `builder_candidate_match_ids_filtered_rule_pair_total`
    - `builder_candidate_match_ids_filtered_rule_pair_avg_per_event`
- 검증
  - `python -m py_compile` 통과

## 다음 실행 확인 포인트
- `builder_candidate_match_ids_filtered_rule_pair_total`가 유의미하게 증가하는지
- `builder_add_match_pair_eval_avg_ms_per_event` 감소 여부
- `builder_pair_edges_graph_path_pf_compute_avg_ms_per_event` 동반 하락 여부
- 최종 `events_per_second` 및 `inst_eps` 바닥값 개선 여부
